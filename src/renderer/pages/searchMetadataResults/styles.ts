import { makeStyles, createStyles } from '@material-ui/core/styles';

export default makeStyles(() =>
  createStyles({
    button: {
      marginBottom: '15px',
      marginTop: '15px',
    },
  })
);
