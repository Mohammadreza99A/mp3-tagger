import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  button: {
    marginTop: '15px',
    marginBottom: '15px',
  },

  fileInput: {
    display: 'none',
  },

  fileLabel: {
    width: '100%',
  },
});
