import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
    display: 'inline-block',
    maxWidth: '400px',
    maxHeight: '400px',
  },

  coverImage: {
    maxWidth: '100%',
    height: 'auto',
  },
});
